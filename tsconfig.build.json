// Specialized tsconfig to only compile the .ts files in the src dir
{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "noEmit": false,
        "declaration": false
    },
    "exclude": ["src/**/*.test.ts"]
}
